
<!-- <div class="">
  <h1>carousel</h1>
</div> -->
{% section 'carousel' %}

<div class="">
  <h1>news 1</h1>
</div>

<div class="categories-section">

  <div class="categories">
    {% for collection in collections %}
    <div class="categorie-item">
      <div class="categorie-title">
        <h3>{{ collection.title }}</h3>
      </div>
      <div class="categorie-body">
          <div class="outer-box">
            {% if collection.image %}
            <img class="categorie-picture" src="{{ collection | img_url: 'large' }}" alt="{{ collection.img.alt }}">
            <div class="inner-box">
              <a href="{{ collection.url }}" class="categorie-btn">shop now</a>
            </div>
            {% else %}
            <p>Image not found</p>
            {% endif %}
          </div>
      </div>
      <div class="categorie-description">
        <p>{{ collection.description }}</p>
      </div>
    </div>
    {% endfor %}
  </div>

  <div class="rectangle-decorator"></div>

</div>

<div class="">
  <h1>news 2</h1>
</div>
