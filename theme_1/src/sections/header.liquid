<div data-section-id="{{ section.id }}" data-section-type="header-section">
  <header role="banner">

    <div class="header-first-line grid">
      <div class="header-section-nav grid__item small--one-third medium-up--hide">
        {% include 'icon-hamburger' %}
      </div>

      <div class="header-section-icon grid__item small--one-third medium-up--hide">
        <h1 itemscope itemtype="http://schema.org/Organization">
          <a href="/" itemprop="url" class="site-logo site-header__logo-image">
            <img src="{{ 'logo.png' | asset_url }}" class="small--hide">
            <img src="{{ 'logo-small.png' | asset_url }}" class="medium-up--hide small--show">
          </a>
        </h1>
      </div>

      <div class="header-section-cart grid__item small--one-third medium-up--hide">
        <a href="/cart">         
          <span class="small--hide">{{ 'layout.cart.title' | t }}</span>
          {{ cart.item_count }}
          {% include 'icon-cart' %}
        </a>
      </div>

      <div class="header-section-user grid__item medium-one-whole small--hide">
        <ul>
          {% if customer %}
            <li>
            {% if customer.first_name != blank %}
              {% capture first_name %}<a href="/account">{{ customer.first_name }}</a>{% endcapture %}
              {{ 'layout.customer.logged_in_as_html' | t: first_name: first_name }}
            {% else %}
              <a href="/account">{{ 'layout.customer.account' | t }}</a>
            {% endif %}
            </li>
          {% else %}
            <li>
              {{ 'layout.customer.log_in' | t | customer_login_link }}
            </li>
            <li class="text-color-pink">
              {{ 'layout.customer.create_account' | t | customer_register_link }}
            </li>
          {% endif %}
        </ul>
      </div>

    </div>

    <div class="header-second-line">
      
      <div class="header-section-navbar medium-one-third small--hide">
        <h1 itemscope itemtype="http://schema.org/Organization">
          <a href="/" itemprop="url" class="site-logo site-header__logo-image">
            <img src="{{ 'logo-1280.png' | asset_url }}">
          </a>
        </h1>
      </div>

      <ul class="header-main-nav small--hide">
        <li>Boards</li>
        <li>Goggles</li>
        <li>Gloves</li>
        <li>Helmets</li>
      </ul>

      <form class="search-form" action="/search" method="get" role="search">
        <label for="Search" class="label-hidden">
          {{ 'general.search.placeholder' | t }}
        </label>
        <div class="row">
          <div class="col-lg-12">
            <div class="input-group">
              <input type="text" class="form-control" />
              <button class="input-group-addon" type="submit">
                {% include 'icon-search' %}
              </button>
            </div>
          </div>
        </div>
      </form>

      <div class="header-section-medium-cart small--hide">
        <a href="/cart">     
          {{ cart.item_count }}
          {% include 'icon-cart' %}
        </a>
      </div>

    </div>

  </header>

{% schema %}
  {
    "name": "Header",
    "settings": [
      {
        "type": "image_picker",
        "id": "logo",
        "label": "Logo image"
      },
      {
        "type": "range",
        "id": "logo_max_width",
        "min": 50,
        "max": 450,
        "step": 10,
        "unit": "px",
        "label": "Custom logo width",
        "default": 250
      },
      {
        "type": "link_list",
        "id": "main_linklist",
        "label": "Menu",
        "default": "main-menu"
      }
    ]
  }
{% endschema %}
